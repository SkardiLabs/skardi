metadata:
  name: "search-fulltext"
  version: "1.0.0"
  description: >
    Full-text keyword search via MongoDB $text index.
    The query should be pre-tokenized (space-separated) for Chinese text.

# Parameters:
#   {query} - Search terms (space-separated for OR, "quoted" for phrase)
#   {limit} - Maximum number of results

query: |
  SELECT doc_id, "fullTextToken", _score
  FROM mongo_fts('document_texts', {query}, {limit})
  ORDER BY _score DESC
