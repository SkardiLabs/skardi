## 1. Scaffolding and dependencies

- [x] 1.1 Add `otel` cargo feature to `crates/skardi/Cargo.toml` and the corresponding passthrough feature on `crates/server/Cargo.toml`; gate new code behind `#[cfg(feature = "otel")]`.
- [x] 1.2 Add new dependencies under the feature: `prometheus-http-query` (pinned minor version), promote `reqwest` from optional to feature-gated default, and confirm `humantime-serde` + `url` are reachable for config parsing.
- [x] 1.3 Add `DataSourceType::Otel` to [crates/skardi/src/sources/data_source_type.rs](crates/skardi/src/sources/data_source_type.rs); update `as_str` (`"otel"`), `Display`, and any exhaustive matches surfaced by `cargo check --workspace`.
- [x] 1.4 Create the module tree `crates/skardi/src/sources/providers/otel/` with `mod.rs`, `prometheus.rs`, `loki.rs`, `http.rs`, `time.rs`, `error.rs`, `translator.rs`; export the module from `crates/skardi/src/sources/providers/mod.rs` behind the feature flag.

## 2. Shared HTTP and error infrastructure

- [x] 2.1 In `otel/error.rs` define a `pub enum OtelError` covering `Http`, `UpstreamStatus { status, body }`, `Parse`, `ResultTooLarge`, `WindowTooLarge`, `MissingCredential`, `InlineCredentialRejected`, `Timeout`, `UnsupportedPushdown { source, predicate, hint }`, `Backend` variants; implement `From<OtelError> for DataFusionError` with messages that name the source. For `UnsupportedPushdown`, the `Display` impl SHALL quote the offending predicate and include the `hint` (typically a `prom_query`/`loki_query` invocation that would express the same intent).
- [x] 2.2 In `otel/http.rs` build a shared `OtelHttpClient` struct wrapping `reqwest::Client` with: pre-baked headers from config, bearer/basic auth resolved from env at construction time, per-request timeout, and `extra_headers` map for tenant identification (e.g. `X-Scope-OrgID`).
- [x] 2.3 In `otel/time.rs` provide helpers: `resolve_window(caller_start, caller_end, caller_step, defaults, max)` returning either resolved `(start, end, step)` or `OtelError::WindowTooLarge`; unit-test edge cases (start > end, step zero, window > max, all-defaults).

## 3. Configuration model

- [x] 3.1 Define `OtelSourceConfig` (in `otel/mod.rs`) with serde fields: `backend: OtelBackend`, `url: Url`, `auth: OtelAuth`, `extra_headers: BTreeMap<String, String>`, `max_result_rows: Option<usize>` (default 50_000), `default_window: Option<Duration>` (default 15m), `default_step: Option<Duration>` (default 30s), `max_window: Option<Duration>` (default 24h), `request_timeout: Option<Duration>` (default 10s).
- [x] 3.2 Define `OtelAuth` enum: `None`, `Bearer { env: String }`, `Basic { username_env: String, password_env: String }`. Implement a custom `Deserialize` that rejects any inline `token`/`password` field with a clear error message ("inline secrets are not permitted; use `env:` instead").
- [x] 3.3 Implement `OtelSourceConfig::load_credentials(&self) -> Result<ResolvedAuth, OtelError>` that reads env vars at server-startup time and returns `MissingCredential` if any required var is unset/empty.
- [x] 3.4 Wire `OtelSourceConfig` into `crates/server/src/config.rs` source-loader so `type: otel` parses; add a `crates/server/tests/otel_config.rs` integration test covering: valid Prometheus config, valid Loki config, missing `backend`, unsupported backend, inline-secret rejection, missing env var.

## 3.5. SQL → backend-query translator (shared)

The tier-1 `metrics` / `logs` tables both call into this module; the goal is one place that owns predicate / projection / aggregate pushdown rules.

- [x] 3.5.1 In `otel/translator.rs` define `pub struct PromQuerySpec { selector: String, group_by: Option<Vec<String>>, agg: Option<Aggregate>, window: TimeWindow }` and `pub struct LokiQuerySpec { selector: String, line_filters: Vec<LineFilter>, window: TimeWindow, limit: Option<usize> }` as the IR the providers consume.
- [ ] 3.5.2 Implement `pub fn translate_metrics_filters(filters: &[Expr], group_by: Option<&[Expr]>, agg: Option<&AggregateExpr>, defaults: &TimeDefaults) -> Result<PromQuerySpec, OtelError>`. Match exactly the supported-predicate matrix in `design.md` Decision 4 (required `name = '<metric>'`, `labels['k']` equality/inequality/`LIKE`/`NOT LIKE`, `ts BETWEEN …`/`>=`/`<=`/`=`, `LIMIT`, group-by + `sum`/`avg`/`min`/`max`/`count` over labels). Anything outside the matrix returns `OtelError::UnsupportedPushdown` with the offending `Expr` rendered into the `predicate` field and a copy-pasteable `prom_query('…')` invocation in the `hint`. *(v1 partial: `name='X'`, `ts BETWEEN/≥/≤/>/</=`, `LIMIT` implemented end-to-end with tests; `labels['k']` matchers and `GROUP BY` aggregate pushdown deferred to a follow-up — they need DataFusion's `map_extract` / aggregate-pushdown hook validation against a running provider, which lands in section 4.)*
- [ ] 3.5.3 Implement `pub fn translate_logs_filters(filters: &[Expr], defaults: &TimeDefaults) -> Result<LokiQuerySpec, OtelError>` with the Loki matrix (REQUIRED stream-label equality / regex on `labels['k']`, `line LIKE '%substr%'` / `NOT LIKE`, `ts BETWEEN …`, `LIMIT`). Same `UnsupportedPushdown` contract pointing the caller at `loki_query` / `loki_range`. *(v1 partial: `line LIKE/NOT LIKE` (substring + anchored regex fallback), `ts BETWEEN/≥/≤/>/<`, `LIMIT` implemented; v1 returns the empty selector `{}` until label-matcher recognition lands. Section 5's `LokiLogsTable` rejects empty selectors before they reach upstream.)*
- [x] 3.5.4 Treat SQL `LIKE` as a Prometheus/Loki regex matcher: escape regex metacharacters in the pattern, then translate `%` → `.*` and `_` → `.`. Anchor with `^…$`. Add a focused unit test covering the escape (`LIKE 'a.b%'` must not produce `a.b.*` semantics).
- [x] 3.5.5 Implement `WHERE labels['k'] IN ('a','b','c')` as the equivalent `k=~"a|b|c"` matcher (per design.md Open Questions resolution — strong lean yes). *(Helper `in_list_to_regex` implemented + tested; consumed by `labels['k']` matcher recognition deferred under 3.5.2.)*
- [ ] 3.5.6 Unit-test the translator exhaustively (table-driven tests in `otel/translator.rs`): every supported predicate combination plus a representative unsupported case for each "unsupported" example listed in design.md Decision 4. Each unsupported test SHALL assert the `hint` contains the substring `prom_query(` or `loki_query(` as appropriate. *(v1 coverage matches what 3.5.2/3 actually implement: 31 tests covering all currently-supported shapes plus representative `UnsupportedPushdown` cases — `name`-less metrics, `value > X` fallthrough, aggregate pushdown rejection, ILIKE, Loki ts-eq, arbitrary value predicate. Grows alongside 3.5.2/3 as labels and aggregates land.)*
- [x] 3.5.7 Document the translator's supported matrix as a `///` doc comment on the translator module so `cargo doc` renders it next to the code; the `docs/otel/` page links to this rendered doc.

## 4. Prometheus provider (`metrics` table + escape-hatch functions)

- [x] 4.1 Implement `PromMetricsTable` as a DataFusion `TableProvider` with the fixed schema from Decision 6 (`name: Utf8, labels: Map<Utf8,Utf8>, ts: Timestamp(Millisecond, UTC), value: Float64`). Override `supports_filters_pushdown` to return `Exact` for filters the translator accepts and `Unsupported` for everything else; `scan(state, projection, filters, limit)` calls `translator::translate_metrics_filters` then issues the resulting `/api/v1/query` (instant) or `/api/v1/query_range` (range) call. *(v1: `supports_filters_pushdown` returns `Exact` for everything and lets the translator in `scan()` be the single source of truth — fast-fail on unsupported predicates per design.md Decision 4 is the intended behavior.)*
- [ ] 4.2 Implement aggregate pushdown on `PromMetricsTable`: when DataFusion plans a `GROUP BY` over `name` / `labels['k']` with `sum`/`avg`/`min`/`max`/`count(value)`, recognize the pattern in `scan_with_aggregates` (or DataFusion's equivalent hook depending on version) and emit `<agg> by(<keys>)(<selector>)` instead of fetching raw samples. Falls back to raw scan + DataFusion aggregation when the pattern doesn't match. *(Deferred: depends on 3.5.2's `labels['k']` matcher recognition + DataFusion's aggregate-pushdown hook. v1 ships `Aggregate` enum + `agg`/`group_by` fields on `PromQuerySpec` but the translator currently rejects any non-`None` aggregate with a clear `UnsupportedPushdown` pointing at `prom_query`. Counter-vs-gauge semantic mismatch documented in design.md Decision 4 "v1 sharp edge". Tracked for a follow-up change after metrics/labels pushdown lands.)*
- [x] 4.3 Implement `PromQueryFunction` as the **tier-3 escape hatch** (DataFusion `TableFunctionImpl`) for `prom_query(query: Utf8) -> instant query`; output schema matches `PromMetricsTable`. Converts `prometheus_http_query::response::Data` variants (`Vector`, `Scalar`, `Matrix`) into rows. *(JSON parsing is hand-rolled against the Prometheus HTTP API rather than going through `prometheus-http-query` because the latter constructs its own `reqwest::Client` and would bypass our auth/timeout/extra_headers configuration.)*
- [x] 4.4 Implement `PromRangeFunction` for `prom_range(query, start?, end?, step?)`; default missing arguments via `time::resolve_window`; emit one row per `(series, sample)` pair.
- [x] 4.5 Apply `max_result_rows` cap during batch construction in both the table and the escape-hatch functions; emit `OtelError::ResultTooLarge` once the cap is exceeded rather than truncating silently.
- [x] 4.6 Register the table and the two functions in DataFusion via a `register_prometheus_source(session_ctx, name, config)` function called from the server/CLI source-loader; namespace functions per source if multiple OTEL sources are loaded (e.g. `<source_name>_prom_query`) — or document that table-function names are global and recommend single-source-per-process for v1. *(v1: registers bare `metrics` table + global `prom_query` / `prom_range` UDTFs. Single-source-per-process is the recommended v1 deployment shape; multi-source qualification is a follow-up.)*
- [ ] 4.7 Add `crates/skardi/tests/otel_prometheus.rs` against a mock HTTP server (`wiremock`) covering both tiers:
  - **Tier-1 (`metrics` table):** `SELECT * FROM metrics WHERE name='http_requests_total' AND ts > now() - INTERVAL '1 hour'` → asserts the issued URL is `/api/v1/query_range` with the right `query=`, `start=`, `end=`, `step=` query string; `WHERE labels['service']='api'` → asserts selector becomes `http_requests_total{service="api"}`; `WHERE labels['route'] LIKE '/api/%'` → asserts regex matcher `route=~"^/api/.*$"`; `GROUP BY labels['service'] SUM(value)` → asserts `sum by(service)(http_requests_total{})`; `SELECT * FROM metrics` (no `name`) → asserts `UnsupportedPushdown` with `prom_query` hint; `WHERE value > 0.5` → same fallback.
  - **Tier-3 (escape hatch):** instant query happy path, range query with defaults, range query with explicit args, window-too-large rejection, PromQL parse error surface, row-cap exceeded, request-timeout exceeded, missing env credential at registration.
  *(v1 partial: 8 wiremock tests covering the predicates the v1 translator supports — `name='http_requests_total'` → `/api/v1/query_range`, `name='up' AND ts=...` → `/api/v1/query`, `SELECT * FROM metrics` (no `name`) → `UnsupportedPushdown` with `prom_query` hint, plus tier-3 escape hatch happy path, empty-result, parse error, row-cap exceeded, and `extra_headers` forwarding. Tests for `labels['k']` matchers and `GROUP BY` aggregate pushdown wait on 3.5.2 / 4.2.)*

## 5. Loki provider (`logs` table + escape-hatch functions)

- [x] 5.1 Implement `LokiLogsTable` as a DataFusion `TableProvider` with the fixed schema from Decision 6 (`ts: Timestamp(Nanosecond, UTC), labels: Map<Utf8,Utf8>, line: Utf8, stream: Utf8`). Override `supports_filters_pushdown` per the Loki translator matrix; `scan(state, projection, filters, limit)` calls `translator::translate_logs_filters` and issues `/loki/api/v1/query_range`. *(v1: `supports_filters_pushdown` returns `Exact` for all and lets the translator in `scan()` be the single source of truth. Empty-selector queries — which the v1 translator emits until 3.5.3's label-matcher recognition lands — are rejected at the table boundary with a clear `UnsupportedPushdown` pointing at `loki_range`.)*
- [x] 5.2 Map `WHERE line LIKE '%<substr>%'` to the LogQL line filter `|= "<substr>"` and `WHERE line NOT LIKE '%<substr>%'` to `!= "<substr>"`. `LIKE` patterns with anchors or non-substring shapes (`LIKE 'foo%'`) translate to the regex form `|~ "^foo.*$"`. Test both shapes. *(Implemented in the translator (3.5.3) + tested at both unit and integration level. The substring fast-path uses `extract_substring_like`; everything else falls through to `like_to_regex`.)*
- [x] 5.3 Implement `LokiQueryFunction` as the **tier-3 escape hatch** for `loki_query(query: Utf8) -> instant log query`; output schema matches `LokiLogsTable`.
- [x] 5.4 Implement `LokiRangeFunction` for `loki_range(query, start?, end?, limit?)`; defaults via `time::resolve_window`; honour Loki's `limit` parameter (max default 1000, configurable). *(v1: forwards caller-supplied `limit` as Loki's `limit=` query param when present. The "max default 1000" cap from the task spec is left to Loki's server-side default rather than enforced client-side — operators tighten via `max_result_rows`.)*
- [x] 5.5 Handle Loki's `streams` and `matrix` response shapes inside the escape-hatch functions (matrix appears for metric-style log queries like `rate({...}[5m])`); for `matrix` reuse the Prometheus value/timestamp schema by returning rows with `line = ""`, `stream = "matrix"`, and a `value` field — confirm during implementation whether to split into a separate function or share `LokiRangeFunction`. The tier-1 `LokiLogsTable` only emits `streams` rows since its translator doesn't produce metric-style LogQL. *(Implementation decision: a single `LokiExec` handles both shapes. The fixed schema has no `value` column, so matrix values are packed into `line` as a string and `stream = "matrix"` marks the row type. Unit-tested in `build_record_batch_from_matrix_packs_value_into_line`.)*
- [x] 5.6 Register the table and functions in DataFusion via `register_loki_source(session_ctx, name, config)`. *(Wired into `crates/server/src/config.rs` — declaring `type: otel` + `backend: loki` in `ctx.yaml` now works end-to-end.)*
- [ ] 5.7 Add `crates/skardi/tests/otel_loki.rs` against `wiremock` covering both tiers:
  - **Tier-1 (`logs` table):** `SELECT * FROM logs WHERE labels['app']='checkout' AND ts > now() - INTERVAL '15 minutes'` → asserts selector `{app="checkout"}` and time params; `… AND line LIKE '%error%'` → asserts `|= "error"`; `SELECT * FROM logs` with no stream-label predicate → `UnsupportedPushdown` with `loki_query` hint; `WHERE line LIKE '%a%' AND line LIKE '%b%'` → asserts both filters chained in LogQL.
  - **Tier-3 (escape hatch):** stream response happy path, empty result returns empty batch (not error), parsed structured fields appear in `labels` Map, window-too-large rejection.
  *(v1 partial: 9 wiremock tests covering — empty-selector rejection (both with and without line-LIKE predicates), tier-3 range happy path with selector + line filter verified via query-param assertions, explicit `limit` forwarded, tier-3 instant happy path, matrix response handling, empty result, upstream parse error, row-cap enforcement. Tests for `labels['k']` matchers and chained line filters via the tier-1 table wait on 3.5.3.)*

## 6. Read-only access mode and SQL validator wiring

- [x] 6.1 In `crates/skardi/src/sources/access_mode.rs` confirm `AccessMode::ReadOnly` exists and the SQL validator already covers it for other read-only sources; if it doesn't, extend `sql_validator.rs` to reject `INSERT`/`UPDATE`/`DELETE`/`COPY INTO` for OTEL-registered tables. *(The existing validator infrastructure already handles `INSERT`/`UPDATE`/`DELETE` against `ReadOnly` tables. v1 contribution: extended `validate_pipeline_sql` in `crates/server/src/config.rs` so OTEL sources register their backend-specific table names — `metrics` for Prometheus, `logs` for Loki — into the validator's access-mode map. Without this, `INSERT INTO metrics` would slip past validation since the validator only knew the source's `name:` field, not the table names actually exposed by the OTEL providers.)*
- [x] 6.2 Add a test in `crates/skardi/tests/otel_access_mode.rs` confirming `INSERT INTO metrics VALUES (...)` is rejected with a clear, source-named error. *(5 tests: SELECT permitted, INSERT/UPDATE/DELETE all rejected with `WriteNotAllowed { operation, table }` naming the actual OTEL table, error-message-contains-table-name assertion.)*

## 7. Server integration and dashboard surface

- [x] 7.1 In `crates/server/src/server.rs` register OTEL sources at startup; surface registration errors in startup logs with the source name. *(Existing `register_data_sources_with_registry` flow already wraps OTEL errors with source-name context via `.with_context("Failed to register data source: <name>")`. v1 contribution: explicit `tracing::info!` on successful OTEL registration matching the Postgres/SQLite/etc. pattern, logging source name, backend, url, and access mode.)*
- [x] 7.2 In `crates/server/src/handlers.rs` + `crates/server/src/gui.rs` ensure the new source type appears in the Semantics dashboard tab with a "read-only" badge and a backend-specific subtitle (`Prometheus @ url`, `Loki @ url`). *(Dashboard template now includes `{{ACCESS_BADGE}}` rendered for every source — `read-only` or `read-write` depending on `access_mode` — and `{{SUBTITLE_BLOCK}}` rendered specifically for OTEL sources as `Prometheus @ <url>` / `Loki @ <url>`. Non-OTEL sources keep their existing rendering with an empty subtitle. The `data_source_type_str` mapping was already extended in section 1.3 to surface "otel".)*
- [x] 7.3 Wire `crates/server/src/metrics.rs` to emit a counter `skardi_otel_queries_total{source, backend, outcome}` and a histogram `skardi_otel_query_duration_seconds{source, backend}` so operators can monitor the new source via the existing observability stack. *(New module [crates/skardi/src/sources/providers/otel/metrics.rs](crates/skardi/src/sources/providers/otel/metrics.rs) defines `OtelMetrics` using `opentelemetry::global::meter` so the instruments share the binary's existing provider. `PromHttpClient::run` + `LokiHttpClient::run` wrap their inner work with `Instant::now()` + `metrics.record(Outcome, elapsed_secs)` so every upstream call updates both instruments. `Outcome::from_error` maps each `OtelError` variant to a stable string label so dashboards can break down failures by root cause. A pointer comment in `crates/server/src/metrics.rs` documents the metrics for discoverability.)*
- [x] 7.4 CLI: in `crates/cli/` register OTEL source kinds so `skardi sql --ctx ctx.yaml '<sql>'` works for ad-hoc queries. *(Added `otel: Option<OtelSourceConfig>` field on `LocalDataSource` gated on `feature = "otel"`, and an `"otel"` arm in `register_source` that mirrors the server's registration flow — same `load_credentials` env-var path, same `OtelHttpClient` construction, same `register_prometheus_source` / `register_loki_source` dispatch. `crates/cli/Cargo.toml` gains an `otel = ["skardi/otel"]` passthrough feature.)*

## 8. Pipelines integration (the user-facing surface)

Per design.md Decision 10, pipeline YAML files are the canonical authoring + invocation surface for OTEL data. This section delivers the pipeline path end-to-end so the demo (`demo/otel_service_health/`) is the model operators copy. No new pipeline kind, no OTEL-specific endpoint — `metrics` / `logs` / `prom_query` etc. land in the existing pipeline `query` field exactly like a Postgres table would.

- [x] 8.1 Confirm in code (`crates/skardi/src/pipeline/`) that pipeline template substitution does not collide with PromQL/LogQL `{ }` characters. **Tier-1 SQL pipelines** (queries that hit `metrics` / `logs` directly) contain no PromQL braces and SHALL work without changes; **tier-3 pipelines** embedding `prom_query('...{...}...')` / `loki_query('...{...}...')` strings need escape handling — implement whichever fix the existing template engine supports (escape sequence, alternate delimiter for OTEL params, or substitute-before-quote) and unit-test both cases. *(Verified: the substituter regex `\{([a-zA-Z_][a-zA-Z0-9_]*)\}` only matches `{identifier}`, which excludes the `=`, `"`, `,` characters PromQL/LogQL selectors always contain (`{service="api"}`) plus the empty `{}` shape. New unit test `test_promql_logql_braces_do_not_collide_with_param_substitution` in [inferencer.rs](crates/skardi/src/pipeline/inferencer.rs) pins this safety property.)*
- [x] 8.2 Add a `demo/otel_service_health/` example whose directory layout matches the existing demos under `demo/*/pipelines/` *(see [demo/otel_service_health/](demo/otel_service_health/) — `ctx.yaml` declares Prometheus + Loki sources, three pipelines plus a README. Pipelines hardcode time bounds and selectors because v1 pipeline param substitution has known limitations (see README's "v1 sharp edges").)*
- [x] 8.3 Add a smoke test under `crates/server/tests/` that boots the server with the demo `ctx.yaml`, calls each pipeline endpoint via the standard pipeline route (`POST /pipelines/<name>` with a JSON body), and asserts a non-empty response. Use `wiremock` to stub Prometheus + Loki so it runs in CI without the docker stack. *([crates/server/tests/otel_demo_smoke.rs](crates/server/tests/otel_demo_smoke.rs) — 3 tests, one per demo pipeline, all green. Test loads the demo pipeline files from disk, registers OTEL sources against wiremock-stubbed Prometheus + Loki, drives requests through the same `configure_routes` router the production binary uses.)*
- [x] 8.4 Update the `docs/otel/` reference (task 9.1) to lead with a pipeline example, not an ad-hoc SQL snippet, so the documented entry point matches the contract in Decision 10. CLI usage (`skardi sql --ctx ctx.yaml ...`) is mentioned only under an "Operator validation during source bring-up" subsection — not as an agent-facing path. *(Implemented in [docs/otel/README.md](docs/otel/README.md) — Quick-start uses a pipeline; CLI is the last section under "Operator validation during source bring-up".)*

## 9. Documentation

- [x] 9.1 Create `docs/otel/README.md` whose **primary worked example is a pipeline file** (not an ad-hoc SQL snippet), with: motivation, supported backends, config reference, schema reference (column types for the `metrics` / `logs` tables and the escape-hatch functions), **the supported-predicate matrix from design.md Decision 4 rendered as a table**, credential handling rules, row-cap + time-window + request-timeout behaviour, two worked pipeline examples (one tier-1 SQL-only that hits `metrics` directly, one tier-3 wrapping `prom_query`), a "when to reach for the escape hatch" decision-tree paragraph, and an "Operator validation during source bring-up" subsection that documents the `skardi sql --ctx ctx.yaml ...` CLI path as a development convenience only.
- [x] 9.2 Update `docs/observability.md` to add a section distinguishing "Skardi emitting OTEL telemetry" (existing content) from "Skardi consuming OTEL telemetry" (new), with a link to `docs/otel/README.md`.
- [x] 9.3 Update top-level `README.md` "Sources supported" coverage (add OTEL / Prometheus / Loki to whichever table or paragraph enumerates the existing sources) and add a single-line bullet to the Roadmap if traces are still pending.
- [x] 9.4 Add a row to [AGENTS.md](AGENTS.md) under the providers list (`crates/skardi/src/sources/providers/otel/`).

## 10. CI, packaging, and ship

- [x] 10.1 Add a CI matrix entry building `cargo check --workspace --features otel` to ensure the feature compiles on every PR. *(New `check-otel` job in [.github/workflows/ci.yml](.github/workflows/ci.yml) runs `cargo check` for both default features and `--features skardi/otel`, then exercises the otel-gated unit + integration test suites without spinning up the full service-container test matrix. ~2-minute fast lane that fails fast on feature-gating regressions.)*
- [x] 10.2 Run `cargo fmt --all`, `cargo clippy --workspace --all-targets --features otel -- -D warnings`, and `cargo test --workspace --features otel`; fix anything that surfaces. *(`cargo fmt` applied across the OTEL files; clippy clean on all new code; full test sweep: 457 default-feature tests pass, 537 skardi-otel unit tests + 35 otel integration tests pass.)*
- [x] 10.3 Tag a `docker-compose` profile under `observability/` (or `demo/otel_service_health/`) that an external user can run to validate the integration against real Prometheus + Loki containers without touching their own infra. *([observability/docker-compose.yml](observability/docker-compose.yml) header comment documents the demo-subset invocation `docker compose -f observability/docker-compose.yml up -d prometheus loki`, which works because the demo only needs the Prometheus + Loki HTTP query endpoints (already exposed at :9090 and :3100 by the existing stack). Avoided introducing service-level `profiles:` tags because they would have changed the default `docker compose up` behavior — see commit notes.)*
- [ ] 10.4 Open the PR with a screencast or transcript of an agent running `service_health` via the Skardi REST endpoint, demonstrating the end-to-end value (PromQL-free agent flow). *(Meta task: ready to open the PR. Implementation cannot do this from the editor — handing off.)*
